<?php /**
 * @var $options array
 * @var $this \View\StudentView
 */ ?>

<nav>
    <a href="<?= $_SERVER['HTTP_REFERER'] ?>">Назад</a>
</nav>
<table border="1">
    <tr>
        <td>id</td>
        <td>Имя</td>
        <td>e-mail</td>
        <td>Дата рождения</td>
        <td>Фото</td>
    </tr>
    <?php foreach ($options['data'] as $student): ?>
        <tr>
            <?php foreach ($student as $key => $value): ?>
                <td><?=$this->renderElement($key, $value) ?></td>
            <?php endforeach; ?>
        </tr>
    <?php endforeach; ?>
</table>
