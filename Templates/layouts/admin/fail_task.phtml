<?php /** @var $options array */ ?>

<nav>
    <a href="<?= $_SERVER['HTTP_REFERER'] ?>">Назад</a>
</nav>

<form action="/admin/task/restart" method="post">
    <table border="1">
        <tr>
            <td>id</td>
            <td>Исполнитель</td>
            <td>Задание</td>
            <td>
                <button type="submit">Возобновить</button>
            </td>
        </tr>
        <?php foreach ($options['data'] as $task): ?>
            <tr>
                <?php foreach ($task as $value): ?>
                    <td><?= $value ?></td>
                <?php endforeach; ?>
                <td><input type="checkbox" name="<?= $task['id'] ?>" value="<?= $task['id'] ?>"></td>
            </tr>
        <?php endforeach; ?>
    </table>
    <br>
    <label>Когда закончится:</label>
    <input type="datetime-local" name="time_end">
</form>
